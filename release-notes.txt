v0.11.2
-------
- UI：会话查看器时间线表头控件高度统一到 22px，工具下拉框不再比搜索框/放大镜高一截
- 修复：opencode provider 在 Windows 上的默认目录改为 %USERPROFILE%\.config\opencode（之前误用 %LOCALAPPDATA%\opencode）
- 修复：AI 辅助分析 preset 列表里 nga 命令名错写成 opencode，改回独立命令名 nga；已有用户 settings.json 在加载时自动迁移（仅当字段还是旧默认值才升级，用户改过则保留）

v0.11.1
-------
- 重构：拆分会话查看器（SessionViewer）单文件 1489 行的混合实现
  - 改建为 components/SessionViewer/ 目录：index.tsx 容器（~570 行）+ stats.ts / viz.ts 纯计算 + parts/ 子组件 + hooks/ 复用逻辑
  - 抽出 useDropdownDismiss 消除 AgentSwitcher 与 ToolFilterDropdown 之间的 click-outside / Esc 处理重复
  - 抽出 useMessageSearch / useSkillUsage 把消息搜索和 skill 用量请求两块独立逻辑从容器组件里剥离
  - 子组件不再 prop-drill `t`，各自直接 useT()；tooltip 渲染从 helper 函数升级为 `<ToolBreakdownTooltip />` / `<SkillBreakdownTooltip />` 组件
  - 行为零变化：仅文件组织调整，所有 UI 路径（消息搜索、子代理切换、tool filter、Ctrl+E、skill tooltip）保持原样

v0.11.0
-------
- 前端国际化（i18n）：默认中文 UI，支持切换到英文
  - 自建轻量 catalog（src/i18n/{zh,en}.ts）+ I18nProvider + useT() hook，TS 字面量类型确保两边 key 同形
  - 设置 → 显示 新增「界面语言」下拉：自动（跟随系统）/ 中文 / English；切换立即生效
  - 首次启动按 navigator.language 探测：以 zh 开头则中文，否则英文
  - AppSettings.ui 新增 language 字段（"auto" | "zh" | "en"），与 settings.json 一起持久化
  - 覆盖范围：菜单栏 / 工具栏 / 状态栏 / 会话列表 / 会话查看器（指标面板、时间线、子代理切换、工具过滤、tooltip）/ 全部对话框（设置、关于、反馈、AI 分析、远程主机编辑、远程同步进度、provider splash、更新提示）
  - format.ts 的相对时间桶（5 分钟前 / 5m ago）按当前语言渲染
  - Rust 抛上来的错误消息保留原文（不本地化）；release notes 不双语，按作者写的中文显示

v0.10.1
-------
- 文档：按当前代码重写 README.md
  - opencode backend 状态由 stub 改为 Implemented（SQLite at ~/.local/share/opencode/opencode.db）
  - 补齐近几版新增能力：会话内消息搜索、Skill 用量统计、AI 辅助分析、远端 SSH 同步与缓存、会话导出、aaa-hub（自动更新 + 反馈）、Win98 主题
  - Architecture 树同步 core/{remote,feedback,log_buffer,log_excerpt,logger,stats}、src-tauri/{hub,hub_commands}、server/、docs/、平台脚本与 vendor 缓存目录
  - 快捷键表勘误：Ctrl+F 聚焦会话内消息搜索框，Ctrl+Alt+F 聚焦会话列表过滤框，新增 Ctrl+Shift+E 导出
  - Run 段指向实际的 dev/build-release 脚本而不是裸 npx tauri
- 仓库：禁止 git 任何形式的行尾改写
  - 新增 .gitattributes：`* -text`（所有文件按二进制对待，checkin/checkout 都不转换 EOL）
  - 本地 git config：core.autocrlf=false / core.eol=lf / core.safecrlf=false

v0.10.0
-------
- 桌面端接入 aaa-hub：自动检查更新（tauri-plugin-updater）+ 反馈对话框 + "我的反馈"列表
- Toolbar 新增 Feedback 按钮；hub 不可达时自动灰显，tooltip 提示原因
- 顶部 UpdateBanner：启动 5 秒后探测；有新版本时显示「立即安装/稍后」，下载进度可视化
- FeedbackDialog 表单：必填 category/title/description；选填 severity/contact_email/截图（PNG/JPG ≤10MB）
- 自动附带预览面板（可逐项取消）：版本号 / OS / 近期日志摘要（已脱敏） / 设备 id（匿名 ULID）
- 设置面板新增「Hub」与「我的反馈」两个 tab；保存设置后自动 refreshHub + 重新探测连通性
- 网络失败一律静默：仅 console.info / log::info；UI 不弹任何错误 toast 或对话框
- 新增 5 个 Tauri 命令：hub_status / submit_feedback / get_feedback_status / list_local_tickets / refresh_hub
- 注册 tauri-plugin-updater + tauri-plugin-process（用于 relaunch）
- AppSettings.hub { base_url, device_id }；首次启动生成匿名 ULID 设备 id

v0.9.0
------
- aaa-hub 服务端骨架完工：Rust/Axum/SQLite 单二进制（server/ workspace 成员，自身版本 0.1.0）。
- API：/healthz、/v1/updates/manifest、/v1/updates/artifacts/*、/v1/feedback CRUD（含 multipart 附件）、/admin/api/* 全套（list/patch/download/publish）。
- 反馈鉴权：匿名 ticket + 32 字节 base64url claim_token，constant-time 校验。
- 升级：semver 扫 artifacts_dir，每平台返回离线生成的 .sig 内容；ServeDir 直接静态分发产物。
- Admin：Bearer token + 简单 SPA（admin-ui/index.html + admin.js，无前端构建链）。
- 通知：lettre 异步 SMTP，3 次指数退避，失败仅记日志。
- 限流：governor keyed limiter，POST /v1/feedback 按小时、GET manifest 按分钟，X-Forwarded-For 优先。
- 18 个集成/单元测试覆盖 health/config/db/feedback CRUD/attachments/manifest/notify/admin/ratelimit。
- 桌面端尚未接入；下一版完成 HubClient + UpdateBanner + FeedbackDialog + FeedbackList。

v0.8.2
------
- 新增设计文档 docs/aaa-hub-update-feedback-server-design.md：服务端 aaa-hub 的整体架构、API、数据模型、客户端集成原则。
- 新增实现计划 docs/aaa-hub-implementation-plan.md：26 个任务的 TDD 步骤分解，覆盖服务端到桌面端的接入。

v0.8.1
------
- 修复消息搜索框旁边的 🔍 按钮高度比输入框矮 3px 不对齐：按钮在 .th-msg-search 作用域内显式锁定 22px 高度。
- 修复 About 对话框 release-notes 区域被 maxHeight: 240 截断、下方留出大片空白的问题：modal-body 改为 flex 列布局，release-notes 块自动撑满剩余高度。

v0.8.0
------
- 会话视图新增「消息搜索框 + 搜索按钮」：在当前会话/子代理的消息时间线里全文匹配（含 text/thinking/note/tool 输入输出/附件路径），按钮点击或回车触发搜索；连续点击在多条命中之间循环跳转，命中节点会自动展开并滚动到位；未命中时输入框背景闪红，下次编辑自动恢复。
- 快捷键调整：Ctrl+F 改为聚焦「会话内消息搜索框」（更贴近浏览器/编辑器的"页面内查找"语义）；Ctrl+Alt+F 聚焦左侧会话列表过滤框；Toolbar / View 菜单中的提示文案同步更新。

v0.7.0
------
- About 对话框：新增「关于 AAA」窗口，展示软件名称、版本、作者（Yin Yuhao）、简介与当前版本 release-notes。
- 版本号从 Cargo 包元数据自动注入，避免硬编码漂移。
- 新增 Tauri 命令 get_app_info：把版本号、release-notes 在编译期通过 env! / include_str! 内联到二进制。
- CLAUDE.md 提交约束补充：每次 bump 版本号必须同步追加 release-notes.txt 条目。
